<Page
    x:Class="HudlRT.Views.MainView"
    IsTabStop="false"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:caliburn="using:Caliburn.Micro"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="140" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="GoBack" Style="{StaticResource BackButtonStyle}"/>
            <TextBlock Text="HudlRT" Style="{StaticResource PageHeaderTextStyle}" Grid.Column="1" />
        </Grid>
        <TextBlock HorizontalAlignment="Left" Margin="83,34,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Main Page" VerticalAlignment="Top" Height="30" Width="125" FontFamily="Global User Interface" FontSize="22"/>



    </Grid>
</Page>