<UserControl
    x:Class="HudlRT.Views.GameView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HudlRT.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:caliburn="using:Caliburn.Micro"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <TextBlock Style="{StaticResource Section_TextBlockStyle_Standard}" x:Name="Date" />
            <TextBlock Style="{StaticResource Section_TextBlockStyle_Standard}" x:Name="Hyphen" Text="-" />
            <TextBlock Style="{StaticResource Section_TextBlockStyle_Standard}" x:Name="Opponent" />
        </StackPanel>
            <ListView  IsItemClickEnabled="True" Background="Transparent" Foreground="White" Name="Categories" SelectedItem="{Binding SelectedCategory, Mode=TwoWay}" caliburn:Message.Attach="[Event ItemClick] = [CategorySelected($eventArgs)]"
                   Margin="35,0,0,0" ItemContainerStyle="{StaticResource Section_ListViewItemStyle_Cutups}" IsHoldingEnabled="False" IsSwipeEnabled="False" SelectionChanged="ListViewSelectionChanged"></ListView>
    </StackPanel>
</UserControl>
