<Page
    x:Class="HudlRT.Views.VideoPlayerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HudlRT"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:caliburn="using:Caliburn.Micro"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Name="transportStyle" TargetType="Button">
            <Setter Property="Height" Value="40" />
            <Setter Property="Width" Value="75" />
            <Setter Property="FontSize" Value="11" />
        </Style>
    </Page.Resources>

    <Page.BottomAppBar>
        <AppBar Name="appBar" Background="White" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnPlay_AppBar" Click="btnPlay_Click" Style="{StaticResource PlayAppBarButtonStyle}" />
                <Button x:Name="btnPause_AppBar" Click="btnPause_Click" Style="{StaticResource PauseAppBarButtonStyle}"/>
                <Button x:Name="btnStop_AppBar" Click="btnStop_Click" Style="{StaticResource AppBarButtonStyle}"
                                        AutomationProperties.Name="Stop" Content="[]"/>
                <Button x:Name="btnReverse_AppBar" Click="btnReverse_Click" Style="{StaticResource SkipBackAppBarButtonStyle}"/>
                <Button x:Name="btnForward_AppBar" Click="btnForward_Click" Style="{StaticResource SkipAheadAppBarButtonStyle}"/>
                <Button x:Name="btnMute_AppBar" Click="btnMute_Click" Style="{StaticResource AppBarButtonStyle}"
                                AutomationProperties.Name="Mute" Content="X"/>
                <Button x:Name="btnFullScreenToggle_AppBar" Click="btnFullScreenToggle_Click"
                                    Style="{StaticResource AppBarButtonStyle}" AutomationProperties.Name="Full Screen" Content="[-]" />
                <Button x:Name="btnVolumeUp_AppBar" Click="btnVolumeUp_Click"
                                    Style="{StaticResource AppBarButtonStyle}"  AutomationProperties.Name="Volume" Content="+"/>
                <Button x:Name="btnVolumeDown_AppBar" Click="btnVolumeDown_Click" Style="{StaticResource AppBarButtonStyle}"
                                    AutomationProperties.Name="Volume" Content="--" />
                <ProgressBar x:Name="vol_AppBar" Height="15" Width="80" Background="White" FlowDirection="LeftToRight" Value="100" />
            </StackPanel>
        </AppBar>
    </Page.BottomAppBar>

    <Grid x:Name="mainGrid" Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="140" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Name="header">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button x:Name="GoBack" Style="{StaticResource BackButtonStyle}" />
            <Image Grid.Column="1" Source="Assets/Hudl_logo.jpg" HorizontalAlignment="Left" />
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="480" />
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ContentControl x:Name="videoContainer"
                            KeyUp="VideoContainer_KeyUp"
                            HorizontalContentAlignment="Center" 
                            VerticalContentAlignment="Center" 
                            Height="400" Grid.Row="0" 
                            DataContext="{Binding Video}">
                    <MediaElement Name="videoMediaElement"
                              MediaOpened="videoElement_MediaOpened" 
                              MediaEnded="videoMediaElement_MediaEnded" 
                              MediaFailed="videoMediaElement_MediaFailed"
                              CurrentStateChanged="videoMediaElement_CurrentStateChanged"
                              PosterSource="Assets/Hudl_logo.png"
                              AutoPlay="False" 
                              Source="{Binding fileLocation}"/>
                </ContentControl>
                <!-- Transport Controls -->
                <StackPanel Name="TransportControlsPanel"
                    Grid.Row="1"
                    HorizontalAlignment="Center" 
                    Width="600">
                    <Slider Name="timelineSlider" Margin="10,0" Width="600"/>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button x:Name="btnPlay" Click="btnPlay_Click" Style="{StaticResource PlayAppBarButtonStyle}" />
                            <Button x:Name="btnPause" Click="btnPause_Click" Style="{StaticResource PauseAppBarButtonStyle}"/>
                            <Button x:Name="btnStop" Click="btnStop_Click" Style="{StaticResource AppBarButtonStyle}"
                                    AutomationProperties.Name="Stop" Content="[]"/>
                            <Button x:Name="btnReverse" Click="btnReverse_Click" Style="{StaticResource SkipBackAppBarButtonStyle}"/>
                            <Button x:Name="btnForward" Click="btnForward_Click" Style="{StaticResource SkipAheadAppBarButtonStyle}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button x:Name="btnMute" Click="btnMute_Click" Style="{StaticResource AppBarButtonStyle}"
                                AutomationProperties.Name="Mute" Content="X"/>
                            <Button x:Name="btnFullScreenToggle" Click="btnFullScreenToggle_Click"
                                    Style="{StaticResource AppBarButtonStyle}" AutomationProperties.Name="Full Screen" Content="[-]" />
                            <Button x:Name="btnVolumeUp" Click="btnVolumeUp_Click"
                                    Style="{StaticResource AppBarButtonStyle}"  AutomationProperties.Name="Volume" Content="+"/>
                            <Button x:Name="btnVolumeDown" Click="btnVolumeDown_Click" Style="{StaticResource AppBarButtonStyle}"
                                    AutomationProperties.Name="Volume" Content="--" />
                            <ProgressBar x:Name="vol" Height="15" Width="80" Background="White" FlowDirection="LeftToRight" Value="100" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Grid>
            <ListView x:Name="Clips" Grid.Column="1" IsItemClickEnabled="True" SelectionChanged="VideosList_SelectionChanged" caliburn:Message.Attach="[Event ItemClick] = [ClipSelected($eventArgs)]">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding name}"
                               FontSize="24" Margin="5,0,0,0" TextWrapping="Wrap" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
        </Grid>

    </Grid>
</Page>
