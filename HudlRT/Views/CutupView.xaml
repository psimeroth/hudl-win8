<UserControl
    x:Class="HudlRT.Views.CutupView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HudlRT.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:caliburn="using:Caliburn.Micro">
    
    <Grid Height="150">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="180" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="100" />
      </Grid.ColumnDefinitions>
        <Image Source="{Binding Thumbnail}" Opacity="{Binding Opacity}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0" Height="125" />
        <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" Margin="10,0,0,0">
            <TextBlock Style="{StaticResource Section_TextBlockStyle_Title}" x:Name="Name" Margin="0,0,0,-10" />
            <TextBlock Style="{StaticResource Section_TextBlockStyle_Standard}" x:Name="ClipCountDisplay" Margin="0,-10,0,0" />
        </StackPanel>
    <Image Source="ms-appx:///Assets/selectArrow.png" Height="100" Grid.Column="2" VerticalAlignment="Center" Margin="0,0,20,0" HorizontalAlignment="Right"/>
    </Grid>
</UserControl>
